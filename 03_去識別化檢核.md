# 去識別化：技術上要做的三件事

> 呼應 W02 學習契約：資料保密是紅線。原則上週講完了，這裡講技術上怎麼真的做到。

## 一、清除 DICOM 標頭

姓名、病歷號、生日、檢查號、機構欄位、UID 與 private tags 都可能包含識別資訊。
**使用機構核准且依 DICOM 保密設定檔驗證過的批次流程，不要手動刪除少數欄位。**

以下程式只示範課堂上的「代號取代」概念，**不是可直接用於臨床資料的完整去識別化工具**：

```python
import pydicom
REMOVE = ["PatientName", "PatientID", "PatientBirthDate", "AccessionNumber",
          "InstitutionName", "InstitutionAddress", "ReferringPhysicianName",
          "StudyID", "OtherPatientIDs", "PatientAddress", "PatientTelephoneNumbers"]

def deid_header(path_in, path_out, subject_id):
    ds = pydicom.dcmread(path_in)
    for tag in REMOVE:
        if tag in ds:
            delattr(ds, tag)
    ds.PatientID = subject_id          # 例：'SUB001'
    ds.PatientName = subject_id
    ds.remove_private_tags()
    ds.save_as(path_out)
```

這段範例沒有完整處理所有日期、描述文字、序號、UID 關聯與廠商私有欄位。正式研究應：

- 依 IRB、資料使用協議及機構政策決定保留、移除或一致替換的欄位。
- 保持 Study／Series／SOP UID 的內部關聯，同時避免可回推原始系統。
- 批次完成後產生欄位稽核報告，並抽樣人工檢查。
- 將去識別化程式版本、規則與處理日期寫入不可變更的稽核紀錄。

## 二、檢查像素本身

部分超音波／內視鏡會把病人資訊**燒進畫面**。標頭清乾淨了，畫面左上角還有名字 —— 這是最常被忽略的漏洞。清完標頭一定要抽樣把影像打開來看。

## 三、對照表分開存放

代號與真實身分的對照，**不與影像放在同一個空間**，也不進版本控制（見 `.gitignore`）。

## 額外

頭部 3D 影像可重建臉部，公開前須做去臉（defacing）。

---

W07（AI 輔助資料整理與品質控制）會把這一段展開成八項檢核表。
