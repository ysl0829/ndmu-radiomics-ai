# 命名約定｜本課程統一

> 約定的價值不在「哪一種比較好」，在「大家都一樣」。

| 規則 | 做法 | 為什麼 |
|---|---|---|
| 檔名字元 | 只用英數字與底線 | 中文、空白、括號跨系統會出問題 |
| 日期 | `YYYYMMDD`，放最前面 | 排序就是時間順序，例：`20260917_features.csv` |
| notebook | `編號_動作_對象` | 例：`01_preprocess_ct.ipynb`。編號讓別人不用問「先跑哪個」 |
| 結果檔 | 帶日期與參數 | 不要每次覆寫同一個 `result.csv`，例：`20260917_features_bin25.csv` |
| 病人 | 一律用代號 `SUB001` | **絕不使用病歷號或姓名**，這是紅線 |

程式裡直接用 `src.paths.stamped()` 產生檔名，不要每次手打：

```python
from src.paths import stamped
stamped("features_bin25")          # → '20260917_features_bin25.csv'
```

`stamped()` 會拒絕中文、空白、不符合 `YYYYMMDD` 的日期，以及不合法的副檔名，
讓命名錯誤在輸出前就被發現：

```python
stamped("model_auc", date="20261231", ext=".csv")  # ✓
stamped("模型結果")                                # ✗ ValueError
```
