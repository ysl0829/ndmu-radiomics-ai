# 專案資料夾結構｜六層

用 `python scripts/make_project_structure.py` 一鍵建立，或在 notebook 裡：

```python
from src.paths import get_root, ensure_project, show_tree
ROOT = get_root(project="radiomics_course")  # Colab 會自動掛載 Drive
ensure_project(ROOT)
show_tree(ROOT)
```

| 資料夾 | 放什麼 | 可否分享 | 注意事項 |
|---|---|---|---|
| `data/raw/` | 去識別化後的原始影像 | 組內 | **唯讀，永不修改** |
| `data/processed/` | 前處理後的中間檔 | 組內 | 可重建，不必備份 |
| `notebooks/` | 編號的分析 notebook | 可公開 | 不得含病人資料輸出 |
| `src/` | 共用函式與模組 | 可公開 | 同一段程式貼第三次就搬進來 |
| `results/` | 特徵矩陣、模型結果、圖 | 視情況 | 檔名帶日期與參數 |
| `docs/` | README、環境說明、決策紀錄 | 可公開 | W18 驗收先看這個 |

## 完成後的範例

```text
radiomics_course/
├── data/
│   ├── raw/
│   │   └── SUB001/                    # 去識別化原始資料；唯讀、不進 Git
│   └── processed/
│       └── SUB001/                    # 可由程式重建
├── notebooks/
│   ├── 00_w03_setup.ipynb
│   ├── 01_preprocess_ct.ipynb
│   └── 02_model.ipynb
├── src/
│   ├── paths.py
│   └── repro.py
├── results/
│   ├── YYYYMMDD_env.json
│   └── YYYYMMDD_model_auc_ci.csv
└── docs/
    └── README.md
```

範例中的 `SUB001/` 與結果檔只是位置示意。公開 repository 只保留資料夾與說明，
不要加入真實影像、特徵矩陣、身分對照表或 notebook 中的病人輸出。

## 一條規矩

**notebook 裡不准出現完整的絕對路徑。** 全部用 `ROOT` 組合：

```python
out = ROOT / "results" / stamped("features_bin25")   # ✓
out = "/content/drive/MyDrive/我的資料/features.csv"   # ✗ 換一台電腦就死
```

`/content` 底下的東西斷線就消失，中間結果一律存 Drive。
