# W03 交付前檢查紀錄

此檔記錄目前教材實際完成的檢查，避免把「曾經測過」誤寫成「目前仍然通過」。

## 2026-09-16 更新

### 已完成

| 項目 | 方法 | 結果 |
|---|---|---|
| Python 語法 | 以 `ast.parse()` 檢查 `src/*.py` 與 `scripts/*.py` | 6 個檔案通過 |
| Shell 語法 | `bash -n scripts/install_colab.sh` | 通過 |
| notebook 結構 | 解析 JSON、確認 notebook v4 與 cell 類型 | 3 本通過 |
| notebook 程式語法 | 移除行首 `!`／`%` 後逐格 `ast.parse()` | 通過 |
| notebook 必要內容 | `scripts/_build_notebooks.py` 檢查新版內容標記 | 3 本通過 |
| 專案結構工具 | 在暫存目錄執行 `make_project_structure.py` | 六層結構與 `docs/README.md` 均建立 |
| 檔名驗證 | 測試合法名稱及中文、空白、路徑穿越字串 | 非法名稱均被拒絕 |
| 種子與環境紀錄 | 建立環境 JSON，再由 `check_repro()` 讀回檢查 | 通過 |
| 共用環境清單 | 檢查 `requirements.txt` 的精確版本與 PyRadiomics commit | W03–W18 使用同一份清單 |
| Git diff 格式 | `git diff --check` | 通過 |

### 本次修正

1. `scripts/_build_notebooks.py` 原本內嵌舊版 notebook；執行後會覆寫學生版與教師版。現在改為驗證工具，
   預設不寫入，只有 `--clear-outputs` 會清除執行輸出。
2. `install_colab.sh` 改用目前 Python 的 `-m pip`，加入 `pip check`，並驗證核心與
   PyRadiomics 匯入。
3. `make_project_structure.py` 明確區分本機、指定路徑與 Colab，並預設保留既有 `docs/README.md`。
4. `stamped()` 現在檢查英數底線名稱、`YYYYMMDD` 日期及副檔名格式。
5. `check_repro()` 除了種子，也會確認 `results/YYYYMMDD_env.json` 可讀、欄位完整且種子一致。
6. 去識別化文件已明確標示簡化程式不等同完整 DICOM 保密流程。
7. 新增 `data/` 與 `results/` 的安全使用說明及可追蹤空目錄。
8. 環境鎖定檔已統一為 `requirements.txt`，作為 W03–W18 的唯一安裝清單。

## 尚未在本機取代 Colab 驗證的項目

- Colab GPU 配額、`nvidia-smi` 與 PyTorch CUDA 整合。
- Google Drive 實際授權及重新掛載。
- 從公開 GitHub URL 在全新 Colab runtime 執行 `git clone`。
- 在全新環境重新編譯 PyRadiomics。

這些項目依賴 Colab、Drive、網路或編譯環境，課前仍應在實際授課環境執行一次。

## 課前檢查

1. 將 `YOUR-GITHUB-ACCOUNT` 換成可供學生存取的實際 repository URL。
2. 在全新 Colab runtime 執行 `00_w03_setup.ipynb`。
3. 重啟 runtime 後從安裝格重跑，確認環境可重建。
4. 執行 `run_all(ROOT)`，確認環境 JSON 與六層結構通過。
5. 以學生帳號測試 repository 權限，不使用教師已登入的權限判斷。
6. 確認公開 repository 不含 `teacher/`、真實資料、身分對照表或 notebook 病人輸出。
